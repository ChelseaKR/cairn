# Layer: `siskiyou`

Reviewed front-matter markdown documents for this layer go here, with
`tables/*.csv` under it; `assemble_corpus.py` copies them into a corpus
directory and `cairn index` never reads this directory directly. This file
is ignored by both (a `README.md` is never a corpus document). Source list:
`corpus/pilot-ca/sources/siskiyou.toml`. Empty as of 2026-08-23 — see
`docs/pilot-ca.md` for why, per layer.
